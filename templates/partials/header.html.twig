<nav class="navbar navbar-expand-lg navbar-dark bg-primary px-0" style="position:sticky;top:0;z-index:500;">
    <div class="container">
        <a class="navbar-brand" href="{{ path('home')}}">Accueil</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-left navbar-nav mr-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('restaurant_index')}}">Restaurants</a>
                </li>
                {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{path('item_index')}}">Produits</a>
                    </li>
                {% endif %}
            </ul>
            {% if app.user %}
            <ul class="navbar-right navbar-nav ml-auto">
                <li class="nav-item dropdown">
                    <p>Connecté(e) en tant que {{app.user.email}}</p>
                    <a href="{{path('logout')}}" class="dropdown-item"><i class="fas fa-sign-out-alt"></i> Déconnexion</a>
                </li>
                {% else %}
                <ul class="navbar-left navbar-nav ml-auto">
                    <li class="nav-item">
                        <a href="{{ path('register') }}" class="nav-link"><i class="fas fa-user-plus"></i> Inscription</a>
                    </li>
                    <li class="nav-item">
                        <a href="{{ path('login') }}" class="nav-link"><i class="fas fa-sign-in-alt"></i> Connexion</a>
                    </li>

                    {% endif %}
                </ul>
        </div>
    </div>
</nav>

{% for label, messages in app.flashes %}
    <div class="container">
        <div class="alert alert-{{ label }}">
            {% for message in messages %}
                <p>{{ message | raw}} </p>
            {% endfor %}
        </div>
    </div>
{% endfor %}