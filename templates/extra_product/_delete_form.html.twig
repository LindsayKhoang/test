<form method="post" action="{{ path('extra_product_delete', {'id': extra_product.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ extra_product.id) }}">
    <button class="btn">Delete</button>
</form>
