{% extends 'base.html.twig' %}

{% block title %}ExtraProduct{% endblock %}

{% block body %}
    <h1>ExtraProduct</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ extra_product.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ extra_product.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ extra_product.description }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ extra_product.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('extra_product_index') }}">back to list</a>

    <a href="{{ path('extra_product_edit', {'id': extra_product.id}) }}">edit</a>

    {{ include('extra_product/_delete_form.html.twig') }}
{% endblock %}
